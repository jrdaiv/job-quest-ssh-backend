# from . import ma
# from marshmallow import fields

# class TipSchema(ma.Schema):
#     tip_id = fields.Integer(required=False)
#     tip = fields.String(required=True)
#     mission_id = fields.Integer(required=False)

# tip_schema = TipSchema()  
# tips_schema = TipSchema(many=True)